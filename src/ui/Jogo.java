package ui;

import javax.swing.*;

public class Jogo extends JFrame {
    public Jogo() {
        setSize(800, 600);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setVisible(true);
    }
}
